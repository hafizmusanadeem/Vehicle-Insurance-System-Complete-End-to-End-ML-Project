[pytest]
# Adding src folder to Python path so imports work properly
pythonpath=src

# Telling pytest where to find tests
testpaths=src/tests

# Verbose output for clearer test results
addopts=-v

# Recognize files starting with test_ or ending with _test.py
python_files=test_*.py *_test.py
